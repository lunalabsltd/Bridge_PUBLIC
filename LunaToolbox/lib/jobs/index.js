const { build } = require( './build' );
const { updateConfig, validateConfig, getConfig } = require( './config' );

module.exports = {
    build,
    updateConfig,
    validateConfig,
    getConfig,
};
